"use strict";
NEXUS_SPECTRA.Views.HomeDashboard=(()=>{const esc=s=>(s??"").toString().replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c])),peso=n=>new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP",maximumFractionDigits:0}).format(Number(n||0)),date=v=>v?new Date(v).toLocaleDateString("en-PH",{month:"short",day:"numeric",year:"numeric"}):"—";function render(d){document.getElementById("homeMetrics").innerHTML=`<article><span>Active Billing Cases</span><strong>${d.active}</strong><small>${d.total} total registered</small></article><article class="${d.forCompliance?'attention':''}"><span>Exceptions / Compliance</span><strong>${d.forCompliance}</strong><small>requires follow-up</small></article><article><span>Paid / Closed</span><strong>${d.paid}</strong><small>${peso(d.paidClaim)} processed</small></article><article><span>Approved RQMs</span><strong>${d.rqmCount}</strong><small>${d.enrollmentCount} scholar enrollment(s)</small></article><article><span>Scholar Registry</span><strong>${d.scholarCount}</strong><small>individual provincial records</small></article>`;document.getElementById("homeProcessGrid").innerHTML=d.stages.map(s=>`<a class="process-card ${s.tone}" href="${esc(s.url)}"><span class="process-icon" data-process-icon="${esc(s.key)}"></span><span class="process-copy"><strong>${esc(s.label)}</strong><small>${esc(s.description)}</small></span><b>${s.count}</b><em>Open workspace →</em></a>`).join("");document.getElementById("homeAttention").innerHTML=d.attention.length?d.attention.map(n=>`<a class="attention-row ${esc(n.severity)}" href="${esc(n.url||'#')}"><span></span><div><strong>${esc(n.title)}</strong><small>${esc(n.message)}</small></div><b>→</b></a>`).join(""):'<div class="home-empty"><strong>No urgent item.</strong><span>Actionable exceptions and deadlines will surface here.</span></div>';document.getElementById("homeRecent").innerHTML=d.recent.length?d.recent.map(t=>`<div class="recent-row"><div><strong>${esc(t.transactionNo)}</strong><small>${esc(t.tvi)} · ${esc(t.qualification)}</small></div><div><span>${esc(String(t.status||"").replaceAll("_"," "))}</span><small>${date(t.updatedAt||t.createdAt)}</small></div></div>`).join(""):'<div class="home-empty">No billing transactions yet.</div>';document.getElementById("homeFinancial").innerHTML=`<div><span>Total Registered Claim Value</span><strong>${peso(d.totalClaim)}</strong></div><div><span>Paid / Closed Claim Value</span><strong>${peso(d.paidClaim)}</strong></div><div><span>Active ≥ 7 Days</span><strong>${d.oldAging}</strong></div>`;NEXUS_SPECTRA.Views.TVIMonitor.render(d.tvi);decorate();}
 function decorate(){const svg={receiving:'<svg viewBox="0 0 24 24"><path d="M5 4h14v16H5zM8 8h8M8 12h8M8 16h5"/></svg>',scholarship:'<svg viewBox="0 0 24 24"><path d="m12 3 9 5-9 5-9-5zM5 11v5c3 2 11 2 14 0v-5"/></svg>',accounting:'<svg viewBox="0 0 24 24"><path d="M5 3h14v18H5zM8 8h8M8 12h8M8 16h4"/></svg>',budget:'<svg viewBox="0 0 24 24"><rect x="3" y="6" width="18" height="13" rx="2"/><path d="M7 6V4h10v2M8 12h8"/></svg>',cashier:'<svg viewBox="0 0 24 24"><path d="M4 7h16v12H4zM7 7V4h10v3"/><circle cx="12" cy="13" r="2.5"/></svg>'};document.querySelectorAll('[data-process-icon]').forEach(x=>x.innerHTML=svg[x.dataset.processIcon]||'');}
 return Object.freeze({render});})();