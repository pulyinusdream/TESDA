"use strict";
NEXUS_SPECTRA.Views.ProviderMISReview=(()=>{const esc=s=>(s??"").toString().replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
 function list(){const rows=NEXUS_SPECTRA.Services.ProviderMISReview.all().sort((a,b)=>String(b.submittedAt).localeCompare(String(a.submittedAt))),h=document.getElementById("providerMisList");h.innerHTML=rows.length?`<div class="table-wrap"><table><thead><tr><th>Submitted</th><th>Provider / RQM</th><th>File</th><th>Scholars</th><th>Comparison</th><th>Status</th><th></th></tr></thead><tbody>${rows.map(x=>`<tr><td>${new Date(x.submittedAt).toLocaleString("en-PH")}</td><td><strong>${esc(x.provider)}</strong><small>${esc(x.rqmCode)}</small></td><td>${esc(x.fileName)}</td><td>${x.comparison.uploadedCount}</td><td>${x.comparison.mismatches.length} difference(s)</td><td><span class="role-status">${esc(x.status.replaceAll("_"," "))}</span></td><td><button class="role-secondary" data-provider-mis-open="${esc(x.submissionId)}">Review</button></td></tr>`).join("")}</tbody></table></div>`:'<div class="role-empty">No provider MIS submission yet.</div>';}
 function detail(id){const x=NEXUS_SPECTRA.Services.ProviderMISReview.all().find(r=>r.submissionId===id),h=document.getElementById("providerMisDetail");if(!x)return;h.innerHTML=`<div class="role-card"><div class="role-card-head"><div><span class="eyebrow">PROVIDER MIS REVIEW</span><h2>${esc(x.rqmCode)}</h2><p>${esc(x.provider)} · ${esc(x.fileName)}</p></div><span class="role-status">${esc(x.status.replaceAll("_"," "))}</span></div><div class="role-kpis"><article><span>Uploaded</span><strong>${x.comparison.uploadedCount}</strong></article><article><span>Official</span><strong>${x.comparison.officialCount}</strong></article><article><span>Matched</span><strong>${x.comparison.matched}</strong></article><article class="${x.comparison.mismatches.length?'warn':''}"><span>Differences</span><strong>${x.comparison.mismatches.length}</strong></article></div><div class="table-wrap"><table><thead><tr><th>Type</th><th>Scholar</th><th>Field / Detail</th><th>Uploaded</th><th>Official</th></tr></thead><tbody>${x.comparison.mismatches.map(m=>`<tr><td>${esc(m.type.replaceAll("_"," "))}</td><td>${esc(m.scholar)}</td><td>${esc(m.field||m.detail||"")}</td><td>${esc(m.uploaded||"")}</td><td>${esc(m.official||"")}</td></tr>`).join("")||'<tr><td colspan="5">No differences.</td></tr>'}</tbody></table></div><div class="role-form-grid"><div class="role-field span-2"><label>Scholarship Review Remarks</label><textarea id="providerMisRemarks">${esc(x.reviewRemarks||"")}</textarea></div></div><div class="role-actions"><button class="role-primary" data-provider-mis-decision="VERIFIED" data-provider-mis-id="${esc(id)}">Verify MIS</button><button class="role-danger" data-provider-mis-decision="FOR_PROVIDER_CORRECTION" data-provider-mis-id="${esc(id)}">Return for Correction</button></div></div>`;}
 return Object.freeze({list,detail});})();