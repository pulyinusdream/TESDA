"use strict";
NEXUS_SPECTRA.Views.NotificationCenter=(()=>{const esc=s=>(s??"").toString().replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));const dt=v=>{if(!v)return "";const d=new Date(v),diff=Math.floor((Date.now()-d.getTime())/60000);if(diff<1)return "Just now";if(diff<60)return `${diff} min ago`;if(diff<1440)return `${Math.floor(diff/60)} hr ago`;if(diff<10080)return `${Math.floor(diff/1440)} day(s) ago`;return d.toLocaleDateString("en-PH",{month:"short",day:"numeric",year:"numeric"});};
 function icon(sev){return sev==="critical"?"!":sev==="warning"?"•":"i";}
 function render(data){const badge=document.getElementById("notificationBadge"),list=document.getElementById("notificationList"),count=document.getElementById("notificationCount");if(badge){badge.textContent=data.unread>99?"99+":String(data.unread);badge.hidden=!data.unread;}if(count)count.textContent=data.unread?`${data.unread} unread`:`All caught up`;if(!list)return;list.innerHTML=data.items.length?data.items.slice(0,40).map(n=>`<button class="notification-item ${n.read?"read":""} ${esc(n.severity)}" data-notification-id="${esc(n.id)}" ${n.screen?`data-notification-screen="${esc(n.screen)}"`:""} ${n.url?`data-notification-url="${esc(n.url)}"`:""}><span class="notification-severity">${icon(n.severity)}</span><span class="notification-copy"><strong>${esc(n.title)}</strong><span>${esc(n.message)}</span><small>${n.role?`${esc(n.role)} · `:""}${esc(dt(n.createdAt))}${n.ref?` · ${esc(n.ref)}`:""}</small></span>${n.read?"":'<i class="notification-unread"></i>'}</button>`).join(""):'<div class="notification-empty"><strong>No notifications right now.</strong><span>New transactions, deadlines and action items will appear here.</span></div>';}
 return Object.freeze({render});})();