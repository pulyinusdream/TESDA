"use strict";
NEXUS_SAM.Modules.RQM=(()=>{const KEY="NEXUS:SPECTRA:rqmRegistry",PROFILE="NEXUS:SAM:providerProfile",norm=s=>String(s||"").trim().toUpperCase().replace(/\s+/g," ");function all(){try{return JSON.parse(localStorage.getItem(KEY)||"[]");}catch(_){return []}}function provider(){try{return JSON.parse(localStorage.getItem(PROFILE)||"{}");}catch(_){return {}}}function saveProvider(name){const p={tviName:String(name||"").trim(),updatedAt:new Date().toISOString()};localStorage.setItem(PROFILE,JSON.stringify(p));return p;}function mine(){const n=norm(provider().tviName);return all().filter(x=>norm(x.tvi)===n&&!/ARCHIVED|CANCELLED/i.test(x.status||"")).sort((a,b)=>String(a.rqmCode||a.rqmNo).localeCompare(String(b.rqmCode||b.rqmNo)));}function get(rqm){const k=norm(rqm);return all().find(x=>norm(x.rqmNo||x.rqmCode)===k)||null;}return Object.freeze({all,provider,saveProvider,mine,get,norm});})();