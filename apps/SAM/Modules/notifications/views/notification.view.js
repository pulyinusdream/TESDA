"use strict";
NEXUS_SAM.Modules.Notifications.View=(()=>{const esc=s=>(s??"").toString().replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c])),dt=v=>v?new Date(v).toLocaleString("en-PH",{dateStyle:"medium",timeStyle:"short"}):"";function render(d){const badge=document.getElementById("samNotificationBadge"),list=document.getElementById("samNotificationList"),count=document.getElementById("samNotificationCount");if(badge){badge.textContent=d.unread>99?"99+":d.unread;badge.hidden=!d.unread;}if(count)count.textContent=d.unread?`${d.unread} unread`:"All caught up";if(list)list.innerHTML=d.items.length?d.items.slice(0,30).map(n=>`<button class="sam-notification-item ${n.read?"read":""} ${esc(n.severity)}" data-sam-notification="${esc(n.id)}"><span class="sam-notification-dot"></span><span><strong>${esc(n.title)}</strong><small>${esc(n.message)}</small><em>${esc(dt(n.createdAt))}${n.ref?` · ${esc(n.ref)}`:""}</em></span>${n.read?"":'<i></i>'}</button>`).join(""):'<div class="sam-notification-empty">No notifications right now.</div>';}
return Object.freeze({render});})();