"use strict";
NEXUS_SAM.Modules.TIP.View=(()=>{const esc=s=>(s??"").toString().replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));const dt=v=>v?new Date(v).toLocaleString("en-PH",{dateStyle:"medium",timeStyle:"short"}):"";
 function render(rows,notifs){const h=document.getElementById("tipRequestHistory");if(h)h.innerHTML=rows.length?rows.map(r=>`<article class="tip-row"><div><strong>${esc(r.tipRequestNo)}</strong><small>${esc(r.qualification)} · requested ${esc(r.requestedDate)} ${esc(r.requestedTime)}</small></div><div><span class="badge badge-neutral">${esc(r.status.replaceAll("_"," "))}</span>${r.scheduledDate?`<small>Schedule: ${esc(r.scheduledDate)} ${esc(r.scheduledTime)}</small>`:""}${r.status==="SCHEDULED"&&!r.schoolConfirmed?`<button class="btn btn-primary btn-sm" data-tip-confirm="${esc(r.tipRequestNo)}">Confirm Schedule</button>`:""}</div></article>`).join(""):'<div class="empty-state compact">No TIP request for this batch.</div>';const n=document.getElementById("schoolNotifications");if(n)n.innerHTML=notifs.length?notifs.map(x=>`<article class="school-notification ${x.read?"read":""}"><strong>${esc(x.title)}</strong><p>${esc(x.message)}</p><small>${esc(dt(x.createdAt))}</small></article>`).join(""):'<div class="empty-state compact">No new scholarship notifications.</div>'; }
 return Object.freeze({render});})();